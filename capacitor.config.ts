import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'king-hunter.project.noticias',
  appName: 'Noticias',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
